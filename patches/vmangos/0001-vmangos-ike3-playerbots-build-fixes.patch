diff --git a/CMakeLists.txt b/CMakeLists.txt
index ecdf0bd01..44783a6dc 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -113,6 +113,10 @@ if(UNIX)
   find_package(MySQL REQUIRED)
   find_package(OpenSSL REQUIRED)
   find_package(ZLIB REQUIRED)
+  if(TARGET ZLIB::ZLIB AND NOT TARGET zlib)
+    add_library(zlib INTERFACE)
+    target_link_libraries(zlib INTERFACE ZLIB::ZLIB)
+  endif()
 endif()
 
 if(REQUIRES_LIBCURL)
diff --git a/src/game/CMakeLists.txt b/src/game/CMakeLists.txt
index 15e6f9b8a..fe81f36cc 100644
--- a/src/game/CMakeLists.txt
+++ b/src/game/CMakeLists.txt
@@ -576,9 +576,9 @@ endif()
 
 if(BUILD_PLAYERBOTS)
   include_directories(
-    ${CMAKE_CURRENT_SOURCE_DIR}/Playerbots
-    ${CMAKE_CURRENT_SOURCE_DIR}/Playerbots/playerbot
-    ${CMAKE_CURRENT_SOURCE_DIR}/Playerbots/ahbot
+    ${CMAKE_CURRENT_SOURCE_DIR}/PlayerBots
+    ${CMAKE_CURRENT_SOURCE_DIR}/PlayerBots/playerbot
+    ${CMAKE_CURRENT_SOURCE_DIR}/PlayerBots/ahbot
   )
   add_definitions(-DENABLE_PLAYERBOTS)
 endif()
@@ -631,7 +631,7 @@ target_link_libraries(game
 )
 
 if(BUILD_PLAYERBOTS)
-  add_subdirectory(Playerbots)
+  add_subdirectory(PlayerBots)
   target_link_libraries(game playerbots)
 endif()
 
diff --git a/src/game/PlayerBots/playerbot/PlayerbotAI.cpp b/src/game/PlayerBots/playerbot/PlayerbotAI.cpp
index 004622013..66830e1e2 100644
--- a/src/game/PlayerBots/playerbot/PlayerbotAI.cpp
+++ b/src/game/PlayerBots/playerbot/PlayerbotAI.cpp
@@ -1683,7 +1683,7 @@ void PlayerbotAI::HandleBotOutgoingPacket(const WorldPacket& packet)
                     if (lang == LANG_ADDON)
                         return;
 
-                    if (_strnicmp(message.c_str(), (sPlayerbotAIConfig.toxicLinksPrefix).c_str(), (sPlayerbotAIConfig.toxicLinksPrefix).length()) == 0
+                    if (strnicmp(message.c_str(), (sPlayerbotAIConfig.toxicLinksPrefix).c_str(), (sPlayerbotAIConfig.toxicLinksPrefix).length()) == 0
                         && (GetChatHelper()->ExtractAllItemIds(message).size() > 0 || GetChatHelper()->ExtractAllQuestIds(message).size() > 0)
                         && sPlayerbotAIConfig.toxicLinksRepliesChance)
                     {
diff --git a/src/game/PlayerBots/playerbot/strategy/actions/DebugAction.cpp b/src/game/PlayerBots/playerbot/strategy/actions/DebugAction.cpp
index f99ee3f23..dcfc9c430 100644
--- a/src/game/PlayerBots/playerbot/strategy/actions/DebugAction.cpp
+++ b/src/game/PlayerBots/playerbot/strategy/actions/DebugAction.cpp
@@ -2372,7 +2372,7 @@ bool DebugAction::HandleFind(Event& event, Player* requester, const std::string&
     uint32 entry = stoi(link);
 
     Creature* creature = nullptr;
-    MaNGOS::NearestCreatureEntryWithLiveStateInObjectRangeCheck creature_check(*bot, entry, 1000.0f, true, false);
+    MaNGOS::NearestCreatureEntryWithLiveStateInObjectRangeCheck creature_check(*bot, entry, true, 1000.0f);
     MaNGOS::CreatureLastSearcher<MaNGOS::NearestCreatureEntryWithLiveStateInObjectRangeCheck> searcher(creature, creature_check);
     Cell::VisitGridObjects(bot, searcher, 1000.0f);
 
diff --git a/src/game/PlayerBots/playerbot/strategy/actions/GoAction.cpp b/src/game/PlayerBots/playerbot/strategy/actions/GoAction.cpp
index afeb115d9..3d93a23f3 100644
--- a/src/game/PlayerBots/playerbot/strategy/actions/GoAction.cpp
+++ b/src/game/PlayerBots/playerbot/strategy/actions/GoAction.cpp
@@ -714,7 +714,7 @@ bool GoAction::MoveToGps(std::string& param, Player* requester)
             Vector3 end = path.getEndPosition();
             Vector3 aend = path.getActualEndPosition();
 
-            const const PointsArray& points = path.getPath();
+            const PointsArray& points = path.getPath();
             PathType type = path.getPathType();
 
             std::ostringstream out;
diff --git a/src/game/PlayerBots/playerbot/strategy/actions/LootRollAction.cpp b/src/game/PlayerBots/playerbot/strategy/actions/LootRollAction.cpp
index 05e44a22f..c80a867ab 100644
--- a/src/game/PlayerBots/playerbot/strategy/actions/LootRollAction.cpp
+++ b/src/game/PlayerBots/playerbot/strategy/actions/LootRollAction.cpp
@@ -239,31 +239,12 @@ RollVote RollAction::CalculateRollVote(ItemQualifier& itemQualifier)
 
 bool RollAction::RollOnItemInSlot(RollVote vote, ObjectGuid lootGuid, uint32 slot)
 {
-    Loot* loot = (Loot*)nullptr /* sLootMgr not in vmangos */;
-    if (!loot)
-        return false;
-
-    LootItem* item = ((slot < loot->items.size()) ? &loot->items[slot] : nullptr);
-    ItemPrototype const* proto = sObjectMgr.GetItemPrototype(item->itemid);
-    if (!proto)
-        return false;
-
-    /* GroupLootRoll not in vmangos */
-    if (!nullptr)
-        return false;
+    (void)vote;
+    (void)lootGuid;
+    (void)slot;
 
-    bool didRoll = false;
-
-    if (didRoll)
-    {
-        LootRollMap lootRolls = AI_VALUE(LootRollMap, "active rolls");
-
-        ActiveRolls::CleanUp(bot, lootRolls, lootGuid, slot);
-
-        SET_AI_VALUE(LootRollMap, "active rolls", lootRolls);
-    }
-
-    return didRoll;
+    // vMaNGOS does not expose the loot-roll managers this code path expects.
+    return false;
 }
 
 bool LootRollAction::Execute(Event& event)
diff --git a/src/game/PlayerBots/playerbot/strategy/actions/MovementActions.cpp b/src/game/PlayerBots/playerbot/strategy/actions/MovementActions.cpp
index 1d578bcbd..2f43859a3 100644
--- a/src/game/PlayerBots/playerbot/strategy/actions/MovementActions.cpp
+++ b/src/game/PlayerBots/playerbot/strategy/actions/MovementActions.cpp
@@ -1143,7 +1143,7 @@ bool MovementAction::MoveTo(uint32 mapId, float x, float y, float z, bool idle,
         PathFinder path(mover);
         path.calculate(movePosition.getX(), movePosition.getY(), movePosition.getZ(), false);
         PathType type = path.getPathType();
-        const const PointsArray& points = path.getPath();
+        const PointsArray& points = path.getPath();
         movePath.addPath(startPosition.fromPointsArray(points));
         TravelNodePathType pathType;
         uint32 entry;
@@ -1161,7 +1161,7 @@ bool MovementAction::MoveTo(uint32 mapId, float x, float y, float z, bool idle,
             PathFinder path(mover);
             path.calculate(movePosition.getX(), movePosition.getY(), movePosition.getZ(), false);
             PathType type = path.getPathType();
-            const const PointsArray& points = path.getPath();
+            const PointsArray& points = path.getPath();
             bool foundAggro = false;
 
             for (auto p : points)
diff --git a/src/game/PlayerBots/playerbot/strategy/actions/SayAction.cpp b/src/game/PlayerBots/playerbot/strategy/actions/SayAction.cpp
index 18ddc9724..2c9cd3d8e 100644
--- a/src/game/PlayerBots/playerbot/strategy/actions/SayAction.cpp
+++ b/src/game/PlayerBots/playerbot/strategy/actions/SayAction.cpp
@@ -481,20 +481,20 @@ void ChatReplyAction::ChatReplyDo(Player* bot, uint32 type, uint32 guid1, uint32
 
     ChatChannelSource chatChannelSource = bot->GetPlayerbotAI()->GetChatChannelSource(bot, type, chanName);
 
-    if ((_strnicmp(msg.c_str(), "LFG", 3) == 0 || _strnicmp(msg.c_str(), "LFM", 3) == 0)
+    if ((strnicmp(msg.c_str(), "LFG", 3) == 0 || strnicmp(msg.c_str(), "LFM", 3) == 0)
         && HandleLFGQuestsReply(bot, chatChannelSource, msg, name))
     {
         return;
     }
 
-    if ((_strnicmp(msg.c_str(), "WTB", 3) == 0)
+    if ((strnicmp(msg.c_str(), "WTB", 3) == 0)
         && HandleWTBItemsReply(bot, chatChannelSource, msg, name))
     {
         return;
     }
 
     //toxic links
-    if (_strnicmp(msg.c_str(), (sPlayerbotAIConfig.toxicLinksPrefix).c_str(), (sPlayerbotAIConfig.toxicLinksPrefix).length()) == 0
+    if (strnicmp(msg.c_str(), (sPlayerbotAIConfig.toxicLinksPrefix).c_str(), (sPlayerbotAIConfig.toxicLinksPrefix).length()) == 0
         && (bot->GetPlayerbotAI()->GetChatHelper()->ExtractAllItemIds(msg).size() > 0 || bot->GetPlayerbotAI()->GetChatHelper()->ExtractAllQuestIds(msg).size() > 0))
     {
         HandleToxicLinksReply(bot, chatChannelSource, msg, name);
diff --git a/src/game/PlayerBots/playerbot/strategy/values/LootValues.cpp b/src/game/PlayerBots/playerbot/strategy/values/LootValues.cpp
index b866d7149..2a4d2dabf 100644
--- a/src/game/PlayerBots/playerbot/strategy/values/LootValues.cpp
+++ b/src/game/PlayerBots/playerbot/strategy/values/LootValues.cpp
@@ -399,17 +399,9 @@ void ActiveRolls::CleanUp(Player* bot, LootRollMap& rollMap, ObjectGuid guid, ui
 			continue;
 		}
 
-		/* GroupLootRoll not in vmangos */
-		if (!nullptr)
-		{
-			roll = rollMap.erase(roll);
-			continue;
-		}
-
-		if(guid)
-		{
-			roll = rollMap.erase(roll);
-			continue;
-		}
+        // vMaNGOS does not expose GroupLootRoll, so stale roll state cannot be resolved here.
+        roll = rollMap.erase(roll);
+        continue;
 
 		++roll;
 	}
diff --git a/src/mangosd/CMakeLists.txt b/src/mangosd/CMakeLists.txt
index 94285fa36..ca8debe16 100644
--- a/src/mangosd/CMakeLists.txt
+++ b/src/mangosd/CMakeLists.txt
@@ -78,9 +78,9 @@ endif()
 
 if(BUILD_PLAYERBOTS)
   include_directories(
-    ${CMAKE_SOURCE_DIR}/src/game/Playerbots
-    ${CMAKE_SOURCE_DIR}/src/game/Playerbots/playerbot
-    ${CMAKE_SOURCE_DIR}/src/game/Playerbots/ahbot
+    ${CMAKE_SOURCE_DIR}/src/game/PlayerBots
+    ${CMAKE_SOURCE_DIR}/src/game/PlayerBots/playerbot
+    ${CMAKE_SOURCE_DIR}/src/game/PlayerBots/ahbot
   )
 endif()
 
diff --git a/src/scripts/CMakeLists.txt b/src/scripts/CMakeLists.txt
index 60061e65a..8ff8d5e42 100644
--- a/src/scripts/CMakeLists.txt
+++ b/src/scripts/CMakeLists.txt
@@ -332,9 +332,9 @@ include_directories(
 
 if(BUILD_PLAYERBOTS)
   include_directories(
-    ${CMAKE_SOURCE_DIR}/src/game/Playerbots
-    ${CMAKE_SOURCE_DIR}/src/game/Playerbots/playerbot
-    ${CMAKE_SOURCE_DIR}/src/game/Playerbots/ahbot
+    ${CMAKE_SOURCE_DIR}/src/game/PlayerBots
+    ${CMAKE_SOURCE_DIR}/src/game/PlayerBots/playerbot
+    ${CMAKE_SOURCE_DIR}/src/game/PlayerBots/ahbot
   )
 endif()
 
diff --git a/src/shared/Progression.h b/src/shared/Progression.h
index f9a08bce2..a92c9e08a 100644
--- a/src/shared/Progression.h
+++ b/src/shared/Progression.h
@@ -33,7 +33,7 @@
 // Change this to define which build of the game to emulate.
 // Has an effect on things such as core gameplay mechanics,
 // loading of client data, and network packets structure.
-#define SUPPORTED_CLIENT_BUILD CLIENT_BUILD_1_12_1
+#define SUPPORTED_CLIENT_BUILD 5875
 
 // This defines which client builds the world server will accept.
 #if SUPPORTED_CLIENT_BUILD >= CLIENT_BUILD_1_12_1
